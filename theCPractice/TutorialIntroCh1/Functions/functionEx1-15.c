#include <stdio.h>

/* Rewrite the temperature conversion program of Section1.2 to use a function conversion */

float toCelsius(int n);
float toFahr(int n);

int main(){
  
  

  return 0;
}

float toCelsius(int t_fahr) {
  temp = (5.0/9.0) * (t_fahr - 32.0);
  return temp;
}

